*** Settings ***

*** Variables ***
# open account bottom sheet, logout modal, forgot password invalid, etc
${platform_obj_modal_bottom_sheet}    
${platform_img_modal_image}       
${platform_txt_modal_title}       
${platform_txt_modal_message}     
${platform_btn_modal_confirm}     
${platform_btn_modal_later}       
